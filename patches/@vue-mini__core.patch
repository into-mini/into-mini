diff --git a/dist/vue-mini.esm-bundler.js b/dist/vue-mini.esm-bundler.js
index f45c3d996ff2abdf7c175b3d8f6260639c45b453..314094e412483430370c5723ec40685ff9903722 100644
--- a/dist/vue-mini.esm-bundler.js
+++ b/dist/vue-mini.esm-bundler.js
@@ -239,7 +239,7 @@ function inject(key, defaultValue, treatDefaultAsFactory = false) {
 let currentApp = null;
 let currentPage = null;
 let currentComponent = null;
-function getCurrentInstance() {
+export function getCurrentInstance() {
     return currentPage || currentComponent;
 }
 function setCurrentApp(app) {
@@ -357,6 +357,9 @@ function deepToRaw(x) {
         });
         return obj;
     }
+    if (x instanceof Error) {
+        return x;
+    }
     throw new TypeError(`${getType(x)} value is not supported`);
 }
 function deepWatch(key, value) {
@@ -449,7 +452,7 @@ function definePage(optionsOrSetup, config) {
                     return;
                 }
                 data = data || {};
-                data[key] = deepToRaw(value);
+                data[key] = deepToRaw(value) ?? null;
                 deepWatch.call(this, key, value);
             });
             if (data !== undefined) {
@@ -636,7 +639,7 @@ function defineComponent(optionsOrSetup, config) {
                     return;
                 }
                 data = data || {};
-                data[key] = deepToRaw(value);
+                data[key] = deepToRaw(value) ?? null;
                 deepWatch.call(this, key, value);
             });
             if (data !== undefined) {
